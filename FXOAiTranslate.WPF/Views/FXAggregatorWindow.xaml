<Window x:Class="FXOAiTranslate.WPF.Views.FXAggregatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:FXOAiTranslate.WPF.Controls"
        mc:Ignorable="d"
        Title="FX Aggregator (WPF)" 
        Height="850" Width="1000"
        Background="{StaticResource BrushBackgroundMain}"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/FXOAiTranslate.WPF;component/Styles/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/><!-- Top Bar -->
            <RowDefinition Height="*"/> <!-- Main Content -->
        </Grid.RowDefinitions>

        <!-- TOP BAR -->
        <Border Grid.Row="0" Background="{StaticResource BrushBackgroundHeader}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="0,0,0,1" Padding="15,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Logo / Title -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="FXO" FontSize="18" FontWeight="Bold" Foreground="{StaticResource BrushAccentBlue}"/>
                    <TextBlock Text="Ai" FontSize="18" FontWeight="Light" Foreground="{StaticResource BrushTextPrimary}" Margin="2,0,0,0"/>
                </StackPanel>

                <!-- Current Time / Connection Status (Placeholder) -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="#33198754" CornerRadius="10" Padding="10,4" Margin="0,0,10,0">
                         <TextBlock Text="Connected" Foreground="{StaticResource ColorStatusFilled}" FontWeight="Bold" FontSize="11"/>
                    </Border>
                    <TextBlock Text="mgu74" Foreground="{StaticResource BrushTextSecondary}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- MAIN LAYOUT -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>   <!-- Quote Legs Area -->
                <ColumnDefinition Width="350"/> <!-- Deals Panel -->
            </Grid.ColumnDefinitions>

            <!-- LEFT: QUOTE AREA (Scrolling if multiple legs) -->
            <ScrollViewer Grid.Column="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                <StackPanel Orientation="Horizontal">
                    <!-- For now, we manually place one LegPanel. In real MVVM, this would be an ItemsControl binding to Legs collection -->
                    <controls:LegPanel Width="500" Margin="10"/>
                    
                    <!-- Placeholder for potential second leg (Multi-leg support) -->
                    <!-- <controls:LegPanel Width="400" Margin="10,10,0,10" Opacity="0.5"/> -->
                </StackPanel>
            </ScrollViewer>

            <!-- RIGHT: BLOTTER -->
            <controls:DealsPanel Grid.Column="1" Margin="0,10,10,10"/>
            
        </Grid>
    </Grid>
</Window>

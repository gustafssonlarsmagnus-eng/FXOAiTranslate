<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ============================================ -->
    <!-- DATA GRID STYLES                            -->
    <!-- ============================================ -->

    <Style x:Key="QuoteDataGrid" TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource Brush.PanelBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.TextMain}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource Brush.Border}"/>
        <Setter Property="RowBackground" Value="#FF1A1A1A"/>
        <Setter Property="AlternatingRowBackground" Value="{StaticResource Brush.PanelBackground}"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="CanUserReorderColumns" Value="False"/>
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="SelectionMode" Value="Single"/>
        <Setter Property="FontFamily" Value="Consolas"/>
        <Setter Property="FontSize" Value="12"/>
    </Style>

    <!-- Column Header Style -->
    <Style x:Key="DataGridColumnHeader" TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource Brush.HeaderBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.TextSub}"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
    </Style>

    <!-- Data Grid Row Style with Winner Highlighting -->
    <Style x:Key="QuoteDataGridRow" TargetType="DataGridRow">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.TextMain}"/>
        <Style.Triggers>
            <!-- Winner Highlight -->
            <DataTrigger Binding="{Binding IsBestPrice}" Value="True">
                <Setter Property="Background" Value="{StaticResource Brush.WinnerHighlight}"/>
                <Setter Property="FontWeight" Value="Bold"/>
            </DataTrigger>
            <!-- Hover State -->
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#FF252525"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Bank Name Cell Style (Gold when winner) -->
    <Style x:Key="BankNameCellText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Brush.TextMain}"/>
        <Setter Property="Padding" Value="8,4"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsBestPrice}" Value="True">
                <Setter Property="Foreground" Value="{StaticResource Brush.Gold}"/>
                <Setter Property="FontWeight" Value="Bold"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Number Cell Style (Right-aligned) -->
    <Style x:Key="NumberCellText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Brush.TextMain}"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="Padding" Value="8,4"/>
    </Style>

    <!-- Premium Cell Style (Bold) -->
    <Style x:Key="PremiumCellText" TargetType="TextBlock" BasedOn="{StaticResource NumberCellText}">
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>

</ResourceDictionary>
